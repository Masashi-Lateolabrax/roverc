[project]
name = "roverc"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pygame>=2.6.1",
    "numpy>=1.26",
    "cma>=3.3",
]

[dependency-groups]
dev = [
    "ruff>=0.6",
    "pyright>=1.1.380",
]

[tool.ruff]
line-length = 100
target-version = "py39"
src = ["src/python_client", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = [
    "E501",  # line-length handled by formatter, not the linter
]

[tool.pyright]
include = ["src/python_client", "scripts"]
extraPaths = ["src/python_client"]
pythonVersion = "3.9"
reportMissingImports = "warning"
