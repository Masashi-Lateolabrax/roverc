# camera_node_left

Thin dispatch sketch for the left-side Timer Camera X. All logic lives in
`libraries/camera_common`; this sketch only fixes the role string.

```bash
./flash.sh src/camera_node_left
```
