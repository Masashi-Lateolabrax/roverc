# camera_node_right

Thin dispatch sketch for the right-side Timer Camera X. All logic lives in
`libraries/camera_common`; this sketch only fixes the role string.

```bash
./flash.sh arduino_src/camera_node_right
```
